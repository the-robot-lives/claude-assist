# Project Layout Summary — k8-lib

```
k8-lib/
├── bin/                        # Sourced shell modules (18 files; see layout/bin.md)
├── docs/                       # PROJ-LAYOUT.md + layout/
├── .envrc.k8.dc.example        # Scalar config template (direnv-config)
├── .gitignore
├── Makefile                    # install / test targets
├── README.md                   # Install + config resolution order
└── infra-config.yaml.example   # Structural config template
```
