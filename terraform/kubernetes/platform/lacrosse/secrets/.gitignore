# This directory is re-included by ../.gitignore (!secrets/). Keep the encrypted
# *.sealedsecret.yaml files and templates tracked, but NEVER commit plaintext.
unsealed/
values.env
*.plain.yaml
*.plaintext.yaml
