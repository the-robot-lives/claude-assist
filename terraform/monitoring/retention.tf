# ---------------------------------------------------------------------------
# Data Retention
# ---------------------------------------------------------------------------
# signoz_data_retention removed — the SigNoz provider's retention API
# returns 502 on the current server version. Retention is managed via
# SigNoz server defaults (metrics 30d, traces 15d, logs per config).
