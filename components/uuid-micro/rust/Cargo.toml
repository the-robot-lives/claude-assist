[package]
name = "uuid-micro"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Four-glyph printable Unicode UUID display tokens."

[lib]
name = "uuid_micro"
path = "src/lib.rs"

[dependencies]
uuid = "1"
