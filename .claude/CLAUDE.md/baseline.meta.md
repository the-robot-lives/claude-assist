# baseline.meta.md — variant meta + loss ledger

## Variant
- **slug:** baseline
- **derived from:** — (this IS the level-0 control; verbatim from git)
- **compactness level:** 0
- **style:** plain (original prose, unedited)
- **language:** en
- **size:** 11595 bytes / not-yet-measured tokens  (reference point; delta 0%)

## How it was made
Not made — captured. This is the pre-compression repo-root `CLAUDE.md` exactly as it stood before the corpus existed, typos and all. It is the permanent control and the sole in-corpus recovery source for facts dropped by level-5 variants. **Never edit it.**

## Alterations to raise eval scores
- none — and never any; editing baseline invalidates every ledger measured against it.

## Loss ledger
> Nothing dropped or moved — this is the reference.

| what | why | recoverable-where |
|------|-----|-------------------|
| — (full fact set, verbatim) | level 0 — control | n/a |

Coreference / omission flags: none — original text, including its own pronouns and the stale `protocol/` (singular) Key-Dirs reference, preserved intentionally.

## Eval  (dataset: CLAUDE.md.prompt, run not yet run)
| case id | criterion outcomes | pass? |
|---------|--------------------|-------|
| — | not yet run | — |

- **Aggregate:** not yet run — dataset: CLAUDE.md.prompt
- **required_pass:** not yet run
- **token_cost:** not yet measured
- **reject_if hits:** not yet run

## Adhoc evals
- none

## Notes
**Trinity clause ABSENT by design.** The baseline predates the Trinity requirement; it carries no trinity clause and must not be retrofitted — it exists to measure what every other variant added or removed. The stale `protocol/` (singular, empty) directory reference is likewise preserved as a control artifact; all derivatives correct it to `protocols/`.
