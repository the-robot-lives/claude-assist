erl_crash.dump
nohup.*
.fseventsd
.Spotlight-V100
liquibase.properties
.infisical-secrets.yaml
infisical-secrets.yaml
.venv/
.venv-test/
venv/
.docker-state/
.tmp/
tmp/
.terraform
.secrets/

.nvimlog
.DS_Store
.envrc
.claude/settings.local.json
projects/**/.claude/
docker/**/.claude
.helm-state/
.k8-secrets.yaml
*.pem
*.key

.envrc.*
.rebuild/

# Helm dependency archives (regenerated by `helm dependency update`)
*.tgz

# --- Secrets / credentials (prevent accidental commits) ---
# Generated secrets directory (auto-generated envrc + values)
secrets/
# Environment / dotenv files (keep *.example templates checked in)
.env
.env.*
*.env
!.env.example
!.env.*.example
!.env.sample
# Terraform / OpenTofu state and var files (may embed secrets)
tfplan
*.tfplan
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl.bak
# TLS certs, keystores, and private key material
*.crt
*.cert
*.p12
*.pfx
*.pkcs12
*.jks
*.keystore
*.der
*.csr
# Cloud / cluster credentials
kubeconfig
*.kubeconfig
.kube/config
*-credentials.json
gha-creds-*.json
service-account*.json
# Shell credential caches
.pgpass
.netrc
.npmrc

# project-uplift: render variants + low-value finals stay out of git (full corpus → separate tracking repo)
projects/*/design/asset-prompts/**/.genai.*/
projects/*/design/asset-prompts/**/*.png
