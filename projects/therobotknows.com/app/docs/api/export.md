# Export API

## `GET /api/v1/universes/:universe_id/export`

**Query:** `format=json|markdown` (default `json`)

Returns a universe bundle of entries + links (US-099).

- `format=json` → `application/json`
- `format=markdown` → `text/markdown` editorial dump
