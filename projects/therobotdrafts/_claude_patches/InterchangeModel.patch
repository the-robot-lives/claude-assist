--- orig-InterchangeModel.cs	2026-07-08 13:57:14.688452692 +0000
+++ trd/Assets/Scripts/Authoring/Interchange/InterchangeModel.cs	2026-07-08 13:36:37.023293300 +0000
@@ -29,14 +29,36 @@
         Package, Class, Interface, Enum, Struct, DataType,
         Table,      // ERD entity (EA Class stereotyped «table»)
         Note, Artifact, Boundary, Actor,
-        Unknown
+        Unknown,
+
+        // --- appended for the image / PlantUML import (ordinals above stay stable) ---
+        UseCase,
+        State, StateStart, StateEnd,
+        Decision,           // state/activity choice diamond
+        ForkJoin,
+        Activity,           // activity action node / flowchart process step
+        FlowFinal,
+        Component,
+        Lifeline,           // sequence participant
+        MindNode,           // mindmap topic
+        DeploymentNode,     // deployment `node`
+        Database,           // deployment/infra `database`
+        Cloud,              // deployment `cloud`
     }
 
     public enum IxEdgeType
     {
         Association, DirectedAssociation, Aggregation, Composition,
         Generalization, Realization, Dependency, NoteLink, Extension,
-        Unknown
+        Unknown,
+
+        // --- appended for the image / PlantUML import (ordinals above stay stable) ---
+        Transition,         // state / activity flow arrow
+        Include,            // use-case «include»
+        Extend,             // use-case «extend»
+        MessageSync,        // sequence:  A -> B
+        MessageAsync,       // sequence:  A ->> B
+        MessageReply,       // sequence:  A --> B
     }
 
     public enum IxVisibility { Public, Private, Protected, Package }
