defmodule DesigningDerobot.Organizations.InviteTokens do
  @moduledoc """
  Repo for DesigningDerobot.Organizations.InviteToken
  """
  alias DesigningDerobot.Organizations.InviteToken, as: Entity
  use Noizu.Repo
  def_repo(entity: Entity)
end
