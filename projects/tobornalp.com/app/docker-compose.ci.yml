services:
  postgres:
    image: postgis/postgis:16-3.4-alpine
    environment:
      POSTGRES_USER: therobotplans
      POSTGRES_PASSWORD: tobornalp_ci
      POSTGRES_DB: therobotplans_test
    healthcheck:
      test: pg_isready -U therobotplans
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    healthcheck:
      test: redis-cli ping
      interval: 5s
      timeout: 3s
      retries: 5

  migrations:
    build:
      context: ./backend/db
    environment:
      DB_HOST: postgres
      DB_USER: therobotplans
      DB_PASS: tobornalp_ci
      DB_NAME: therobotplans_test
      DB_PORT: "5432"
    depends_on:
      postgres:
        condition: service_healthy

  backend:
    image: ops.noizu.com/tobornalp.com/backend:${IMAGE_TAG:-latest}
    environment:
      DATABASE_URL: ecto://therobotplans:tobornalp_ci@postgres:5432/therobotplans_test
      DB_HOST: postgres
      DB_USER: therobotplans
      DB_PASS: tobornalp_ci
      DB_NAME: therobotplans_test
      DB_PORT: "5432"
      REDIS_URL: redis://redis:6379/0
      SECRET_KEY_BASE: ci-secret-key-base-must-be-at-least-64-bytes-long-for-phoenix-framework!!
      GUARDIAN_SECRET_KEY: ci-guardian-secret-key-for-testing-only
      PHX_SERVER: "true"
      PHX_HOST: localhost
      PORT: "4000"
    depends_on:
      migrations:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 5s
      timeout: 3s
      retries: 15

  frontend:
    image: ops.noizu.com/tobornalp.com/frontend:${IMAGE_TAG:-latest}
    environment:
      PORT: "3000"
      NEXT_PUBLIC_API_URL: /api
    depends_on:
      backend:
        condition: service_healthy

  nginx:
    image: ops.noizu.com/tobornalp.com/nginx:${IMAGE_TAG:-latest}
    ports:
      - "8094:80"
    depends_on:
      - backend
      - frontend
