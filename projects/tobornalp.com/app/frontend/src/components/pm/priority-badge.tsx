// Barrel kept for backward compatibility — these primitives now live in the shared ui lib.
// Prefer importing from "@/components/ui" in new code.
export { PriorityBadge, StatusBadge, ProgressBar, SectionCard, Empty } from "@/components/ui";
