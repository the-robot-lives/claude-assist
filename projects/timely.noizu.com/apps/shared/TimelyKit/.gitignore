# SPM build cache is covered by the project-level apps/**/.build/ rule.

# Local SQLite store and its WAL/SHM sidecars (none exist yet, but tests
# or a local run against the sync client will create them).
*.sqlite
*.sqlite3
*.db
*-wal
*-shm
